mixin teamMember(member)
  .team-member.pl-2.m-3
    .row
      .col-6
        h5 #{member.firstname} #{member.lastname}
      .col-6
        p.text-right
         | #{member.cash} $ | #{member.points} pts
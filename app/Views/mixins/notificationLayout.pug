mixin notificationLayout(notifications)
  .row
    .col-9
      h2.mb-4 Mes notifications
    if notifications != null
      .col-3.d-flex.align-items-end.justify-content-end
        a(href="/notification/seenAll").text-dark
          h6.mb-4 Voir tout
  #notifs.mx-2
    if notifications == null
      h6 Vous n'avez aucune nouvelle notification
    else
      each notif in notifications
        +notification(notif)